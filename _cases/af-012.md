---
case_id:     AF-012
title:       "Office Metadata Timestamp Contradiction"
category:    "Temporal"
status:      Validated
description: "Filesystem timestamps forged via timestomper.exe; embedded Office core.xml creation time predates the forged SI timestamp in a cross-graph join."
artifacts:
  - Office core.xml
  - $MFT
contributor: "@ioi-framework"
date_added:  2025-01-01
rule_link:   /rules/ioi-012/
tags:
  - timestamp-manipulation
os:          Windows 10
tools_used:
  - timestomper.exe
sample_jsonld:
  - label: Office evaluated graph
    href: https://github.com/ioi-framework/ioi-framework/blob/main/CASES/AF-012/snippets/office_xml_filled.jsonld
  - label: Office XML snippet
    href: https://github.com/ioi-framework/ioi-framework/blob/main/CASES/AF-012/snippets/office_xml_snippet.jsonld
---

## Summary

This case demonstrates timestomping detection using Office XML metadata. Office documents (`.docx`, `.xlsx`, `.pptx`) are ZIP archives containing internal XML metadata (`docProps/core.xml`) that preserves the original creation timestamp. When MFT timestamps are forged using `timestomper.exe`, the internal XML metadata remains unchanged, creating a detectable inconsistency.

## Scenario Steps

**1. Set Up** — Created `secret.docx` on Windows system. Captured original timestamps.

**2. Tampering** — Used `timestomper.exe` to forge timestamps of `secret.docx`. The `SetFileTime` API modifies MFT `$STANDARD_INFORMATION` (0x10) timestamps; internal Office XML metadata remains unmodified.

**3. Evidence Collection** — Extracted `$MFT` using forensic tools. Opened `secret.docx` as a ZIP archive. Extracted `docProps/core.xml` for internal metadata. Compared XML `dcterms:created` with MFT timestamps.

## Ground Truth Criteria

- Office XML metadata (`dcterms:created`) shows original creation time
- MFT `$STANDARD_INFORMATION` (0x10) shows forged (later) timestamp
- XML created time &lt; MFT `$SI` created time indicates timestomping

## Inconsistency Summary

When `timestomper.exe` modifies `secret.docx` timestamps, it only changes the MFT `$STANDARD_INFORMATION` (0x10) attribute. The internal Office XML metadata in `docProps/core.xml` retains the original `dcterms:created` timestamp. Detection: XML metadata shows an earlier creation time than the MFT `$SI` timestamp.

## Detection Logic

```sql
-- Step 1: Get Office XML metadata
FIND office_files
  RETURN filePath, dctermsCreated AS xmlCreated

-- Step 2: Get MFT timestamps for same file
FIND mft_entries WHERE mft_entries.filePath = office_files.filePath
  RETURN created0x10 AS mftSiCreated, created0x30 AS mftFnCreated

-- Detection Rule
IF xmlCreated < mftSiCreated
THEN FLAG 'AF-012: Timestomping Detected via Office XML Metadata'
```
